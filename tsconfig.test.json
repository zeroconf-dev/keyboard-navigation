{
    "extends": "./tsconfig.json",
    "compilerOptions": {
        "noEmit": true,
        "downlevelIteration": true
    },
    "exclude": ["coverage", "dist", "docs", "examples", "package", "node_modules"]
}
